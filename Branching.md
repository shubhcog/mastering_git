## Branching - In git branching means allowing you to create different versions of project.

## Two ways of fix the branch source
- git checkout -b feature-branch (It just creates the branch from the last commit in the current branch)

- git branch new-branch-name source-branch (It creates the new branch with the last commit on the source branch mentioned.)